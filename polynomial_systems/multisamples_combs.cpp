#include <iostream>;
#include <cmath>;
#include <random>;
// an a priori unknown quantity for a desired state
// position of the leader seems important 
void multisubset() {
    int numbers = number[:]
    int subsets = {}
    int output = {}

    for roundcount auto && _range = 9999999 {
        pair_count = {};
        for (index = subsets, subsets.items); {
            for (x : subsets) {
                for (y : subsets) {
                    if (y > x) {
                        pair_count[(x, y)] = pair_count.get((x, y), 0) + 1;
                    }
                }
            }
        }
        pair_count = sort([el : pair_count.keys()]), auto glambda = key[]
    }
}
